# Example secrets file. DO NOT commit real secrets to the repo.
# On Streamlit Cloud go to App Settings -> Secrets and add:
#
# GEMINI_API_KEY="your_api_key_here"
#
# The PHT.py file will pick up the value from st.secrets["GEMINI_API_KEY"]
# if you modify PHT.py to read it (see README). If you don't change PHT.py,
# you must ensure any API keys / config are handled safely inside the app.
